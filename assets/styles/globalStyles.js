import {StyleSheet} from 'react-native';

export const globalStyles = StyleSheet.create({
  backgroundWhite: {
    backgroundColor: '#fff',
  },
  flex: {
    flex: 1,
  },
  flexGrow: {
    flexGrow: 1,
  },
});
